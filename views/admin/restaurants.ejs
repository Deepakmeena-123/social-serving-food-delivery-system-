<% layout('layouts/adminBoilerplate') %>
<div class="container">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="mb-0">Restaurants Management</h1>
    <a href="/admin" class="btn btn-outline-primary">Back to Dashboard</a>
  </div>

  <div class="table-responsive">
    <table class="table table-striped table-hover align-middle">
      <thead class="table-dark">
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>Location</th>
          <th>Status</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% restaurants.forEach(restaurant => { %>
        <tr>
          <td><%= restaurant.username %></td>
          <td><%= restaurant.email %></td>
          <td><%= restaurant.location %></td>
          <td>
            <% if (restaurant.isOpen) { %>
              <span class="badge bg-success">Enabled</span>
            <% } else { %>
              <span class="badge bg-secondary">Disabled</span>
            <% } %>
          </td>
          <td>
            <a href="/admin/restaurants/<%= restaurant._id %>" class="btn btn-sm btn-primary">View</a>
            <form action="/admin/restaurants/<%= restaurant._id %>/toggle" method="POST" class="d-inline">
              <button class="btn btn-sm <%= restaurant.isOpen ? 'btn-warning' : 'btn-success' %>">
                <%= restaurant.isOpen ? 'Disable' : 'Enable' %>
              </button>
            </form>
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>
